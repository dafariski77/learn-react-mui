import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#432a",
      light: "#ee4",
    },
    secondary: {
      main: "#44e",
    },
    otherColor: {
      main: "#12a",
      hover: "#13c3",
    },
  },
});
